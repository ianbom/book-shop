import { Head, Link, router, useForm } from '@inertiajs/react';
import { ImagePlus, Pencil, Trash2 } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Input } from '@/components/ui/input';
import { PageHeader } from '@/components/admin/shared/page-header';
import { formatDate, rupiah } from '@/lib/format';
import admin from '@/routes/admin';
import type { Book, StockMovement } from '@/types/admin';

export default function BookShow({ book }: { book: Book & { stock_movements?: StockMovement[] } }) { const upload = useForm<{ images: File[] }>({ images: [] }); const submit = (event: React.FormEvent) => { event.preventDefault(); upload.post(admin.books.images.store(book.id), { forceFormData: true, preserveScroll: true, onSuccess: () => upload.reset() }); }; const deleteImage = (imageId: number) => { if (confirm('Hapus gambar ini?')) router.delete(admin.books.images.destroy({ book: book.id, image: imageId }), { preserveScroll: true }); }; return <><Head title={book.title} /><main className="flex flex-1 flex-col gap-6 p-4 md:p-6"><PageHeader title={book.title} description={`${book.author} · ${book.isbn || 'ISBN tidak tersedia'}`} actions={<Button asChild><Link href={admin.books.edit(book.id)}><Pencil className="mr-2 size-4" />Edit Buku</Link></Button>} /><div className="grid gap-6 xl:grid-cols-[1.4fr_1fr]"><div className="space-y-6"><Card><CardHeader><CardTitle>Gambar Buku</CardTitle></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2">{book.images?.map((image) => <div key={image.id} className="relative overflow-hidden rounded-lg border"><img src={image.url} alt={image.alt_text || book.title} className="aspect-[4/3] w-full object-cover" /><div className="flex items-center justify-between gap-2 p-2 text-xs">{image.is_primary ? <span className="font-medium text-[#2563EB]">Gambar utama</span> : <span>Urutan {image.sort_order + 1}</span>}<Button type="button" size="icon" variant="ghost" onClick={() => deleteImage(image.id)}><Trash2 className="size-4 text-destructive" /></Button></div></div>)}{!book.images?.length && <p className="text-sm text-muted-foreground">Belum ada gambar.</p>}</CardContent></Card><Card><CardHeader><CardTitle>Tambah Gambar</CardTitle></CardHeader><CardContent><form onSubmit={submit} className="flex flex-col gap-3 sm:flex-row"><Input type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={(e) => upload.setData('images', Array.from(e.target.files ?? []))} /><Button disabled={!upload.data.images.length || upload.processing}><ImagePlus className="mr-2 size-4" />Unggah</Button></form></CardContent></Card><Card><CardHeader><CardTitle>Deskripsi</CardTitle></CardHeader><CardContent><p className="whitespace-pre-wrap text-sm leading-6 text-muted-foreground">{book.description || 'Tidak ada sinopsis.'}</p></CardContent></Card></div><div className="space-y-6"><Card><CardHeader><CardTitle>Ringkasan</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Harga</span><strong>{rupiah(book.price)}</strong></div><div className="flex justify-between"><span className="text-muted-foreground">Stok</span><strong>{book.stock}</strong></div><div className="flex justify-between"><span className="text-muted-foreground">Status</span><strong>{book.is_active ? 'Aktif' : 'Nonaktif'}</strong></div><div><span className="text-muted-foreground">Kategori</span><p className="mt-1">{book.categories?.map((category) => category.name).join(', ') || '-'}</p></div><div className="flex justify-between"><span className="text-muted-foreground">Diperbarui</span><span>{formatDate(book.updated_at)}</span></div></CardContent></Card><Card><CardHeader><CardTitle>Riwayat Stok Terakhir</CardTitle></CardHeader><CardContent className="space-y-3">{book.stock_movements?.map((movement) => <div key={movement.id} className="border-b pb-3 last:border-0"><div className="flex justify-between"><span className="font-medium">{movement.type}</span><span className={movement.quantity >= 0 ? 'text-emerald-600' : 'text-red-600'}>{movement.quantity > 0 ? '+' : ''}{movement.quantity}</span></div><p className="text-xs text-muted-foreground">{formatDate(movement.created_at)}</p></div>) || <p className="text-sm text-muted-foreground">Belum ada riwayat.</p>}</CardContent></Card></div></div></main></>; }
BookShow.layout = { breadcrumbs: [{ title: 'Buku', href: admin.books.index() }, { title: 'Detail Buku', href: admin.books.show(1) }] };
